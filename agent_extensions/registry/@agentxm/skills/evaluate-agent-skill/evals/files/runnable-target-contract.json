{
  "contract_version": "3.0.0",
  "owner": "@example",
  "decision": "Exercise one complete, synthetic authoring-smoke lifecycle without supporting a release or approval decision.",
  "target_binding": {
    "skill_name": "normalize-release-notes",
    "required_result_fields": ["target.source_revision", "target.package_content_identity", "suite.suite_content_identity", "runner.selection_source", "protocol_version", "runner.content_identity", "adapters.host.content_identity", "adapters.host.capabilities", "adapters.grader.content_identity", "adapters.grader.capabilities"]
  },
  "scope": {
    "intended_use": "Provider-free fixture coverage of routing, execution, evidence creation, and bounded conclusions.",
    "stages": ["routing", "execution"],
    "case_source": "Public-safe synthetic release-note requests.",
    "excluded_claims": ["Native host routing", "Production target quality", "Release approval"]
  },
  "unit": "One isolated synthetic attempt at one declared case.",
  "environment": {
    "required_result_fields": ["environment.host", "environment.model", "environment.configuration_identity", "environment.active_catalog", "environment.routing_mode", "environment.sandbox", "authority.policy", "authority.network", "started_at", "ended_at"],
    "routing_boundary": "Expose only the synthetic catalog before selection.",
    "execution_boundary": "Activate only the synthetic target and declared fixture adapter."
  },
  "evidence": {
    "required_per_trial": ["case_id", "stage", "raw_output_or_durable_locator", "observed_selection_or_abstention", "assertion_results", "side_effects", "outcome"],
    "outcomes": ["pass", "fail", "unknown", "harness-error"],
    "storage": "Write routine fixture evidence only to .work/evals."
  },
  "grading": {
    "instrument": "Deterministic fixture routing and assertion grading.",
    "unknown_policy": "Missing adapter or artifact evidence remains unknown.",
    "critical_gates": ["Both synthetic stages retain attributable evidence"],
    "calibration": "The outer suite separately checks artifact presence and missing-evidence behavior."
  },
  "trials": {
    "authoring_smoke_minimum": 1,
    "regression_minimum_per_case": 3,
    "release_evidence_minimum_per_host_model_case": 3,
    "isolation": "Fresh task-local state per attempt.",
    "expansion_rule": "Add trials only when a synthetic mechanics conclusion is unstable."
  },
  "comparison": {
    "baseline": "no-baseline",
    "absence": "This fixture measures mechanics, not comparative value."
  },
  "analysis": {
    "minimum_pass_rate": 1,
    "critical_assertions": [
      { "gate": "Both synthetic stages retain attributable evidence", "case_id": "execute", "assertion": "The execution response is preserved as raw evidence." }
    ],
    "report_by": ["case", "trial", "stage", "failure_class"],
    "routing_metrics": ["trigger_rate"],
    "aggregation_rule": "Keep routing, execution, unknowns, and harness errors separate.",
    "threshold": "Both selected fixture cases pass."
  },
  "estimand": {
    "type": "fixed-suite",
    "sampling_unit": "One synthetic case and trial.",
    "uncertainty": "No population inference; report the bound fixture outcomes only."
  },
  "lifecycle": {
    "preflight_failure": "Reserved with no evidence directory.",
    "terminal_states": ["complete", "failed", "canceled"],
    "retry": "Append attempts without overwrite.",
    "resume": "Require matching identities."
  },
  "provenance": {
    "required_result_fields": ["provenance.case_author", "provenance.runner", "provenance.reviewer", "provenance.grader_identity"],
    "independence": "Same-agent fixture evidence remains authoring smoke."
  },
  "freshness": {
    "expires_after_days": 30,
    "refresh_triggers": ["Fixture target, adapter, evaluator protocol, or evidence layout changes"]
  }
}
