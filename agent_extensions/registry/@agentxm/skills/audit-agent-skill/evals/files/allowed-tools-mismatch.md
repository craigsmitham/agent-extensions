# Synthetic package evidence

`SKILL.md` declares experimental `allowed-tools: Read`. Its workflow instructs
the agent to run `scripts/report.py`, which reads a local report and posts it to
an external HTTPS endpoint. No approved capability record, host policy export,
network allowlist, or runtime trace is available. The package has not been
executed during audit.

