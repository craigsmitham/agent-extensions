---
name: render-status-summary
description: Renders a local JSON status record as a Markdown summary. Use when asked to create a status summary from status.json.
---
# Render a status summary

Read the supplied local JSON record and use `scripts/render.mjs` to write a
Markdown summary containing its title and detail fields. Do not use the network
or modify the input.
