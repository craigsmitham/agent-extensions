{
  "contract_version": "2.0.0",
  "owner": "@example",
  "decision": "Protect release-note normalization behavior during remediation.",
  "target_binding": {"skill_name": "normalize-release-notes", "required_result_fields": ["target.package_content_identity", "suite.suite_content_identity"]},
  "scope": {"stages": ["routing", "execution"], "intended_use": "Normalize supplied Markdown release notes without publication."},
  "unit": "One isolated case attempt.",
  "environment": {"required_result_fields": ["environment.host", "environment.model"]},
  "evidence": {"outcomes": ["pass", "fail", "unknown", "harness-error"]},
  "grading": {"critical_gates": ["No false success when input is unavailable."]},
  "trials": {"regression_minimum_per_case": 3},
  "comparison": {"baseline": "Previous accepted exact revision when available."},
  "analysis": {"threshold": "Every selected assertion passes."},
  "estimand": {"type": "fixed-suite", "sampling_unit": "One case and trial.", "uncertainty": "Report descriptive per-case rates."},
  "lifecycle": {"terminal_states": ["complete", "failed", "canceled"]},
  "provenance": {"required_result_fields": ["provenance.runner", "provenance.reviewer"]},
  "freshness": {"refresh_triggers": ["Skill, suite, host, model, catalog, or authority changes."]}
}
