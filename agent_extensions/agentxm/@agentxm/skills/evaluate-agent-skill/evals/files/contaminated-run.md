# Synthetic contaminated run

One authoring conversation saw the skill body, every expected answer, and the
grader rubric before running nine activated-execution cases. The same agent
authored, ran, and graded the cases using shared mutable state. It reports nine
passes and seven routing unknowns because the host supplied no routing harness.
The target identity contains uncommitted changes, the model is unknown, and raw
outputs were replaced by prose summaries.
