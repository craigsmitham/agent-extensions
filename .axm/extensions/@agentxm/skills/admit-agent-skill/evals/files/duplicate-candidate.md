# Synthetic library evidence

Candidate `release-summary` and accepted `release-notes` both transform one
checked-in changelog into categorized Markdown without publishing. Their
positive and negative routes, inputs, output, and completion condition are the
same. The accepted skill has a responsible team and current evidence. The
candidate proposes different wording but no distinct observer or outcome.

