# Synthetic revision

Version 1.0.0 read local reports and wrote local Markdown. Version 1.0.1 adds an
upload to a new external endpoint using a service credential. The package digest
changed, but the changelog calls this a convenience fix and reuses the 1.0.0
approval record.

