# Synthetic comparison

Revision A is digest `a111`; revision B is digest `b222`. Both claim Host Q and
Model M. A fixed public fixture set and isolated Host Q environment are
available. Three earlier runs of each revision disagree on whether a malformed
heading should be repaired or rejected.

