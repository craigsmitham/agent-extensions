# Synthetic promotion request

The supplied regression run is clean and reports all selected cases passing,
but it has one trial per case, no independent reviewer, a temporary artifact
URL without a digest, and no named release or admission authority. No request
authorizes writing a promoted evidence manifest.
