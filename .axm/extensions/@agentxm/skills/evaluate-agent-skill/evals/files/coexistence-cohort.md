# Synthetic cohort evidence

Candidate digest `c301` passes clear positive, paraphrased positive, adjacent
negative, and execution cases in isolation. Engineering cohort revision `e19`
also contains `release-deploy`. With the cohort's real metadata exposed, 4 of 10
deployment prompts select candidate `release-review` instead of
`release-deploy`. Raw traces and fixed graders are available.

