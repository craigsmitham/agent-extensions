# Synthetic evidence limit

Only prose copied from a claimed `SKILL.md` is available. The registry artifact,
manifest, remaining files, digest, publisher identity, signature, license,
dependencies, and acquisition record are unavailable.

