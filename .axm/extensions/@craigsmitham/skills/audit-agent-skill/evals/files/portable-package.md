# Synthetic package inventory

Package digest `sha256:11112222` came from a signed release by the named
publisher. It contains `SKILL.md`, an MIT `LICENSE`, and a 20-line deterministic
formatter script with no dependencies. The script reads one explicit local file,
writes one explicit output file, rejects path traversal, and performs no network
or subprocess calls. The release is pinned and rollback bytes are retained.

