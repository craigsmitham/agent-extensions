# Selected guide excerpt

This is a synthetic evaluation fixture, not a claim about a real Effect API.

For bounded parallel traversal, the guide uses:

```ts
Effect.forEach(items, worker, { concurrency: 4 })
```
