# Behavioral evaluation cases

The machine-readable authority is `evals.json`. Run each case in fresh,
disposable context with the Gen Stack knowledge support path. The suite covers
bounded diagnosis, mutation refusal, explicit selection, and implicit
abstention for broad Research-like language. Missing side-effect evidence is
`unknown`, never an inferred pass.
