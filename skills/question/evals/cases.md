# Behavioral evaluation cases

The machine-readable authority is `evals.json`. These cases verify that the
retained package is only an explicit legacy compatibility route, delegates to
Research framing, blocks without fresh-context support, remains read-only, and
is absent from normal selection.
